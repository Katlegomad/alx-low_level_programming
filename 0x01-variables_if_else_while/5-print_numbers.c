#include <stdio.h>
/**
 * main -Entry point
 * Prints all single digit nos
 * Return: 0 on success
 */
int main(void)
{
	int i;

	for (i = 0; i != 10; i++)
	{
	printf("%d", i);
	}
	printf("\n");
	return (0);
}
